# Memory Layer (Mem0)

> **Note:** The Mem0 memory layer and associated webapp dependencies have been removed from the active documentation. The `memory/` directory and `memory/service.py` still exist in the repository for reference but are not part of the core training and inference workflow.

For visual inference and breed prediction, use the [Model Tester GUI](model-tester.md) (`test_model.py`).
